stages:
  - test

variables:
  INSTALL_PACKAGES: git gcc make cmake unzip

test_enterprise-1.10:
  stage: test
  tags:
    - docker
  image: centos:7
  variables:
    BUNDLE_VERSION: "1.10.3-68-g618f48d"
  before_script:
    - echo "Using tarantool-enterprise-bundle ${BUNDLE_VERSION}"
    - curl -O -L https://tarantool:${DOWNLOAD_TOKEN}@download.tarantool.io/enterprise/tarantool-enterprise-bundle-${BUNDLE_VERSION}.tar.gz
    - mkdir -p tmp
    - tar -xzf tarantool-enterprise-bundle-${BUNDLE_VERSION}.tar.gz -C ./tmp
    - source ./tmp/tarantool-enterprise/env.sh
    - rm -rf tarantool-enterprise-bundle-${BUNDLE_VERSION}.tar.gz
    - tarantool -V
    - export PATH=$PWD/tmp/tarantool-enterprise:$PATH
    - curl -sL https://rpm.nodesource.com/setup_8.x | bash -
    - yum -y install epel-release
    - yum -y install $INSTALL_PACKAGES nodejs python36-pip
    - pip3.6 install -r test/python/requirements.txt
  script:
    - make ci_prepare
    - make lint test

test_opensource-1.10:
  stage: test
  tags:
    - docker
  image: tarantool/tarantool:1.x-centos7
  before_script:
    - tarantool -V
    - yum -y install $INSTALL_PACKAGES python36-pip
    - pip3.6 install -r test/python/requirements.txt
  script:
    - make ci_prepare
    - make lint test

test_opensource-2.x:
  stage: test
  tags:
    - docker
  image: tarantool/tarantool:2.x-centos7
  before_script:
    - tarantool -V
    - yum -y install $INSTALL_PACKAGES python36-pip
    - pip3.6 install -r test/python/requirements.txt
  script:
    - make ci_prepare
    - make lint test

.e2e-opensource-1.10:
  stage: test
  tags:
    - shell
    - vagrant
  before_script:
    - vagrant up
  script:
    - vagrant ssh default < test/end-to-end.sh
  after_script:
    - vagrant halt
    - vagrant destroy
